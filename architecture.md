# מערכת בקרת בקשות ייצור — ארכיטקטורה

מסמך תכנון למערכת ווב לניהול, מעקב ובקרה על בקשות ייצור במדור, על בסיס הפורמט המחייב של יוסי (16/06/26).

> **גרסה 2** — עודכן אחרי ראיון החלטות (14/07/26). ההחלטות שהתקבלו: פריסה בענן (Vercel),
> חשבונות אישיים ללא תפקידים, הרשמה בקוד הצטרפות, מערכת פסיבית (ללא התראות), Next.js + Postgres.
> גרסה 1 (רשת פנימית, Python stdlib + SQLite) שמורה בהיסטוריית הגיט.

---

## 1. מה המערכת פותרת

היום התהליך חי ב-WhatsApp: פורמט טקסטואלי אחיד, אבל —
- אין מעקב סטטוס אחרי שהבקשה נשלחה (מי בעבודה? מה הושלם? מה מאחר תג"ב?)
- אין דרך לסנן/לתעדף (לפי פרויקט, מיקום ייצור, תג"ב)
- אין אכיפה אמיתית של שלמות הנתונים ("בקשות שלא יכללו את כלל המידע עלולות להתעכב" — אבל אין מנגנון)
- אין היסטוריה/נתיב ביקורת (מי שינה מה ומתי)

המערכת נותנת: קליטת בקשות בטופס, ולידציית שלמות, מחזור חיים מלא (קליטה → אישור → ייצור → סיום),
תור עבודות ממוין לפי דחיפות תג"ב עם התרעות חריגה חזותיות, והיסטוריית שינויים עם זהות אמיתית.

## 2. החלטות מנחות (תוצאת ראיון ההחלטות)

| החלטה | נימוק |
|---|---|
| **פריסה: Vercel + Neon Postgres** | נבחר ע"י דימה. Vercel = serverless ללא דיסק קבוע ⇒ SQLite ירד, Postgres מנוהל נכנס |
| **סטאק: Next.js מלא (App Router, JS)** | המסלול הנתמך של Vercel; קל להרחבה (התראות, דוחות) בעתיד |
| **חשבונות אישיים, בלי תפקידים** | כל מחובר יכול הכול, כולל לאשר. "רק בקר מאשר" נשאר נוהל אנושי. ההיסטוריה נרשמת על שם המחובר — נפתרה בעיית "שם המעדכן" הידני וריבוי השמות |
| **הרשמה בקוד הצטרפות משותף** (`JOIN_CODE`) | באינטרנט פתוח, הרשמה חופשית = זרים רואים הכול. קוד משותף = חסם פשוט שקל לצוות לחיות איתו |
| **פסיבית — ללא התראות** | אין cron ואין שירות מייל. המידע חי במסך. נקודת הרחבה מסומנת |
| **ללא ייבוא WhatsApp** | הוחלט לוותר בשלב זה. נשאר רק ייצוא ("העתק כ-WhatsApp") |
| **UI: mobile-first, ערכת "חמ"ל" כהה** | ההודעות נולדות בטלפון. עיצוב ד' (מבנה) + א' (צבעוניות) מתוך `design/` |
| **כמות = טקסט חופשי** | בדוגמאות: "4", "1", "חלק בודד". אכיפת מספר תגרום לעקיפת המערכת |
| **תאריכים נשמרים ISO** (`YYYY-MM-DD`, TEXT) | פשוט, JSON-friendly, בלי הפתעות timezone של טיפוס DATE |

## 3. מודל הנתונים (Postgres)

```
users
├─ id, username (ייחודי, אנגלית), display_name (עברית — מופיע בהיסטוריה)
├─ password_hash (scrypt, salt:hash), created_at

requests
├─ id, serial (מס' עבודה רץ, #1001...)
├─ project, project_alias        -- "רשום תחת..." (ממצא B2)
├─ subject, quantity (טקסט), requester
├─ approver, approval_date       -- ריק = טרם אושר
├─ location, due_date            -- תג"ב
├─ system_name, external_id      -- הקשר ל-ERP הקיים (#1114)
├─ notes, priority, status
├─ created_at / updated_at / completed_at

history                          -- נתיב ביקורת, append-only
├─ id, request_id → requests
├─ ts, actor (display_name של המחובר), action, field, old_value, new_value
```

### מחזור חיים (status)

```
נקלטה ──► ממתינה לאישור ──► מאושרת ──► בייצור ──► הושלמה
   │              │
   └── מעוכבת (חוסרים) ◄──┘        נדחתה / בוטלה (מכל שלב)
```

- נקלטה עם כל שדות החובה + בקר ותאריך אישור → "מאושרת".
- נקלטה בלי אישור בקר → "ממתינה לאישור".
- חסרים שדות חובה → "מעוכבת — חוסרים" + רשימת החוסרים. זה המימוש של הכלל של יוסי.
- השלמת החוסרים מקדמת אוטומטית; שינוי מהותי (כמות/תג"ב/נושא) אחרי אישור נרשם כ"שינוי אחרי אישור".

### שדות חובה (לפי הפורמט של יוסי)
פרויקט, נושא הייצור, כמות, מכניס עבודה, בקר מאשר + תאריך אישור, מיקום ייצור, תג"ב, שם ייצור במערכת.

## 4. רכיבים

```
┌──────────────────────────── דפדפן (בעיקר טלפון) ────────────────────────────┐
│  Next.js client — עברית RTL, ערכת "חמ"ל" כהה                                │
│  /login  התחברות / הרשמה עם קוד הצטרפות                                     │
│  /       תור עבודות: צ'יפים (מדד+מסנן), חיפוש, פיד לפי דחיפות תג"ב,        │
│          גיליון פירוט (סטטוס/עריכה/היסטוריה/העתק-כ-WhatsApp), טופס בקשה     │
└────────────────────────────────── JSON ─────────────────────────────────────┘
                                     │
┌──────────────────────── Next.js API Routes (Vercel) ────────────────────────┐
│  /api/auth/register|login|logout|me   סשן = עוגייה חתומה HMAC (httpOnly)    │
│  /api/requests  GET(סינון)/POST       כל ה-endpoints דורשים סשן             │
│  /api/requests/[id]  GET/PATCH        actor בהיסטוריה = המשתמש המחובר       │
│  /api/requests/[id]/history, /api/stats                                     │
│  lib/domain.js  חוקי הדומיין (חוסרים, סטטוס קליטה, ייצוא WhatsApp)          │
│  lib/store.js   גישה ל-DB + נתיב ביקורת    lib/auth.js  scrypt + HMAC       │
└─────────────────────────────────────────────────────────────────────────────┘
                                     │ pg (SSL)
                          Neon Postgres (DATABASE_URL)
```

תלויות ריצה: `next`, `react`, `pg` בלבד. סיסמאות ב-scrypt וסשן ב-HMAC — שניהם מ-`node:crypto`,
בלי ספריות auth חיצוניות (החלטה מודעת: פחות תלויות, קוד שקוף; המחיר — אין reset סיסמה בשלב זה).

## 5. פריסה ותפעול

1. **Neon**: יצירת פרויקט Postgres (ב-Vercel Marketplace או ישירות) → `DATABASE_URL`.
2. **Vercel**: חיבור ה-repo. משתני סביבה: `DATABASE_URL`, `SESSION_SECRET` (מחרוזת אקראית ארוכה), `JOIN_CODE`.
3. הסכמה נוצרת אוטומטית בפנייה הראשונה (`CREATE TABLE IF NOT EXISTS`); אפשר גם `npm run db:init`.
4. **פיתוח מקומי**: Postgres מקומי + `.env.local` (ר' `.env.example`), `npm run dev`, `npm run db:seed` לנתוני דוגמה.
5. **בדיקות**: `npm test` (לוגיקת דומיין), ובדיקת E2E ידנית/Playwright מול שרת dev.
6. **גיבוי**: Neon שומר גיבויים/PITR ברמת השירות — לוודא שהתוכנית כוללת את זה.

## 6. Blindspot Pass — ממצאים מתוך הדוגמאות (בתוקף גם בגרסה 2)

| # | ממצא בדוגמאות | ההשלכה על התכנון |
|---|---|---|
| **B1** | "כמות: חלק בודד" — כמות היא לא תמיד מספר | `quantity` טקסט; אין ולידציה מספרית |
| **B2** | "MBaz **(רשום תחת שועל מעופף)**" | שדה `project_alias` נפרד, מוצג ומחופש |
| **B3** | אותו אדם בשמות שונים ("דימה"/"דימה גופמן") | **נפתר**: actor = שם התצוגה של החשבון המחובר |
| **B4** | מיקומי ייצור לא עקביים ("הית מלאכה", "מסגריה / דגמים") | מיקום = בחירה מרשימה נלמדת **או** טקסט חופשי |
| **B5** | "מאשר את הייצור?" — בקשה לפני אישור בפועל | אישור הוא מצב במחזור חיים ("ממתינה לאישור") |
| **B6** | תאריך אישור ≠ תאריך שליחה | שניהם נשמרים; תג"ב שעבר ביום הקליטה נצבע מיד |
| **B7** | פורמטי תאריך מעורבים (22.6 / 15.07.26) | **צומצם**: קלט רק ב-date picker; התצוגה בפורמט המדור |
| **B8** | "#1114" בתוך שם הייצור — מזהה של מערכת אחרת | שדה `external_id` נפרד |
| **B9** | הודעת צרצר חתוכה — שדות חסרים | בקשה חלקית נקלטת כ"מעוכבת", לא נדחית |
| **B10** | תג"ב צמוד לאישור (יומיים) | "תג"ב קרוב" מודגש, לא רק "עבר תג"ב" |

## 7. Unknown Unknowns — סטטוס אחרי הראיון

1. **הזנה כפולה מול ה-ERP** ("שם ייצור במערכת") — **עדיין פתוח.** `external_id` הוא הוו לחיבור עתידי. ההחלטה מה מקור האמת — ארגונית.
2. **מי סוגר בקשות** — **עדיין פתוח.** הפורמט של יוסי נגמר בהגשה; בלי אחראי סגירה הלוח יתמלא "בייצור" נצחיים. המערכת חושפת, לא פותרת.
3. **WhatsApp כמקור אמת מקביל** — **התחדד**: הוחלט לוותר על ייבוא, אז ההזנה ידנית בלבד. הכלל "מה שלא במערכת לא בתור" נהיה קריטי עוד יותר.
4. **אישור בלי תמונת עומס** — פתוח (הרחבה עתידית: עומס לפי מיקום).
5. **שינוי אחרי אישור** — ממומש כסימון בהיסטוריה; מדיניות אישור-מחדש טרם הוגדרה.
6. **רגישות המידע בענן** — **הוכרע חלקית**: אינטרנט פתוח + קוד הצטרפות + הזדהות. שמות פרויקטים ("שועל מעופף") יושבים על Neon/Vercel. אם יש דרישת סיווג — זו שאלה לקב"ט, והתשובה עלולה להחזיר את המערכת לרשת פנימית (גרסה 1 בהיסטוריה).
7. **גיבוי** — עבר לאחריות Neon (PITR); לוודא תוכנית מתאימה.
8. **ריבוי זהויות** — **נפתר** (חשבונות אישיים).
9. **"דחוף" בלי תהליך** — שדה עדיפות קיים; מי מוסמך להשתמש בו — נוהל.
10. **מדידת הצלחה** — אחרי חודש: % בקשות שנקלטו מלאות מהפעם הראשונה, % עמידה בתג"ב.

### נקודת תורפה מודעת שנוספה בגרסה 2
- **אין reset סיסמה.** משתמש ששכח — פותח חשבון חדש עם קוד ההצטרפות (השם התופס נשאר). נסבל בצוות קטן; לתקן אם יכאב.
- **קוד הצטרפות אחד לכולם** — מי שעזב עדיין מכיר אותו. החלפת הקוד = שינוי משתנה סביבה ב-Vercel.

## 8. נקודות הרחבה מסומנות (לא בשלב זה)

- התראות (Vercel Cron + מייל) — הוחלט: פסיבי בשלב זה
- תפקידים/הרשאות (בקר בלבד מאשר), reset סיסמה
- ייבוא WhatsApp (הוסר; הקוד ההיסטורי בגיט אם יחזור הצורך)
- קבצים מצורפים (שרטוטים, שבלונות), עומס לפי מיקום, חיבור ERP דרך `external_id`
