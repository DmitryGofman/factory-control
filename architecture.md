# מערכת בקרת בקשות ייצור — ארכיטקטורה

מסמך תכנון למערכת ווב לניהול, מעקב ובקרה על בקשות ייצור במדור, על בסיס הפורמט המחייב של יוסי (16/06/26).

---

## 1. מה המערכת פותרת

היום התהליך חי ב-WhatsApp: פורמט טקסטואלי אחיד, אבל —
- אין מעקב סטטוס אחרי שהבקשה נשלחה (מי בעבודה? מה הושלם? מה מאחר תג"ב?)
- אין דרך לסנן/לתעדף (לפי פרויקט, מיקום ייצור, תג"ב)
- אין אכיפה אמיתית של שלמות הנתונים ("בקשות שלא יכללו את כלל המידע עלולות להתעכב" — אבל אין מנגנון)
- אין היסטוריה/נתיב ביקורת (מי שינה מה ומתי)

המערכת נותנת: קליטת בקשות (כולל **הדבקה ישירה של הודעת WhatsApp בפורמט הקיים**), ולידציית שלמות, מחזור חיים מלא (קליטה → אישור → ייצור → סיום), לוח בקרה עם התרעות חריגה מתג"ב, והיסטוריית שינויים.

## 2. עקרונות והחלטות מנחות

| החלטה | נימוק |
|---|---|
| **אפס תלויות חיצוניות** — Python 3 stdlib בלבד, SQLite, פרונט וניל | ההנחה: רשת פנימית/סגורה ללא גישה ל-pip/npm/CDN. פריסה = העתקת תיקייה + `python3 server.py` |
| **SQLite כקובץ יחיד** (`factory.db`) | היקף צפוי: עשרות בקשות בחודש, קומץ משתמשים במקביל. גיבוי = העתקת קובץ |
| **WhatsApp נשאר ערוץ הקלט** — המערכת מקבלת אותו, לא נלחמת בו | סיכון האימוץ הכי גדול: אנשים ימשיכו לשלוח בוואטסאפ. לכן: מדביקים את ההודעה → המערכת מפרקת אותה לשדות. וגם כפתור "העתק כהודעת WhatsApp" חזרה |
| **ללא הזדהות בשלב א'** — שדה "בוצע ע"י" חופשי בכל שינוי | רשת פנימית, צוות קטן ומוכר. נקודת הרחבה מסומנת (ראה §8) |
| **כמות = טקסט חופשי** | בדוגמאות: "4", "1", "חלק בודד". אכיפת מספר בלבד תגרום לאנשים לעקוף את המערכת |
| **תאריכים נשמרים ISO** (`YYYY-MM-DD`), הפרסר סולח | בדוגמאות: `22.6`, `21.6`, `6.7.26`, `15.07.26` — שנה דו-ספרתית וחסרה. הפרסר משלים שנה נוכחית ומנרמל |

## 3. מודל הנתונים

```
requests
├─ id            INTEGER PK
├─ serial        INTEGER  -- מס' עבודה רץ (מוצג כ-#1101...), ייחודי
├─ project       TEXT     -- שם הפרויקט
├─ project_alias TEXT     -- "רשום תחת..." (ר' ממצא B2)
├─ subject       TEXT     -- נושא הייצור
├─ quantity      TEXT     -- כמות כטקסט מקורי
├─ requester     TEXT     -- מכניס עבודה
├─ approver      TEXT     -- בקר מאשר
├─ approval_date TEXT     -- ISO, ריק = טרם אושר
├─ location      TEXT     -- מיקום ייצור
├─ due_date      TEXT     -- תג"ב נדרש, ISO
├─ system_name   TEXT     -- שם ייצור במערכת (ה-ERP הקיים)
├─ external_id   TEXT     -- מזהה חיצוני, למשל #1114
├─ notes         TEXT     -- הערות מיוחדות
├─ priority      TEXT     -- רגיל / דחוף / מיידי
├─ status        TEXT     -- ר' מחזור חיים
├─ created_at / updated_at / completed_at

history                       -- נתיב ביקורת, append-only
├─ id, request_id → requests
├─ ts, actor (טקסט חופשי), action, field, old_value, new_value
```

### מחזור חיים (status)

```
נקלטה ──► ממתינה לאישור ──► מאושרת ──► בייצור ──► הושלמה
   │              │                                  
   └── מעוכבת (חוסרים) ◄──┘        נדחתה / בוטלה (מכל שלב)
```

- בקשה שנקלטת עם כל שדות החובה **ויש** בקר+תאריך אישור → נכנסת כ"מאושרת".
- נקלטת בלי אישור בקר → "ממתינה לאישור" (כמו ההודעה של מתן: "מאשר את הייצור?").
- חסרים שדות חובה → "מעוכבת — חוסרים", עם רשימת החוסרים מוצגת. זה המימוש של הכלל של יוסי.
- המערכת לא נועלת מעברים בכוח (צוות קטן, אמון גבוה) — אבל כל מעבר נרשם בהיסטוריה.

### שדות חובה (לפי הפורמט של יוסי)
פרויקט, נושא הייצור, כמות, מכניס עבודה, בקר מאשר + תאריך אישור, מיקום ייצור, תג"ב, שם ייצור במערכת. הערות — רשות ("אין" תקין).

## 4. רכיבים

```
┌────────────────────────────── דפדפן ──────────────────────────────┐
│  SPA וניל, עברית RTL (static/index.html + app.js + style.css)     │
│  • לוח בקרה: כרטיסי מצב, טבלה, סינון (סטטוס/פרויקט/מיקום/חיפוש)   │
│  • חיווי חריגה: אדום=עבר תג"ב, כתום=תג"ב ≤ יומיים                 │
│  • טופס בקשה + ולידציית שלמות + רשימת חוסרים                      │
│  • ייבוא: הדבקת הודעת WhatsApp → פירוק אוטומטי לשדות → אישור      │
│  • ייצוא: כפתור "העתק כהודעת WhatsApp" (הפורמט של יוסי)           │
│  • כרטיס בקשה: פרטים, שינוי סטטוס, עריכה, היסטוריה                │
└──────────────────────────── JSON/REST ────────────────────────────┘
                                 │
┌──────────────────────────── server.py ────────────────────────────┐
│  Python 3 stdlib: http.server (ThreadingHTTPServer) + sqlite3     │
│  • GET/POST /api/requests, GET/PATCH /api/requests/{id}           │
│  • POST /api/parse   ← טקסט WhatsApp, מחזיר שדות מפורקים          │
│  • GET  /api/requests/{id}/history                                │
│  • GET  /api/stats   ← מונים ללוח הבקרה                           │
│  • ולידציה, נרמול תאריכים, כתיבת היסטוריה                          │
└───────────────────────────────────────────────────────────────────┘
                                 │
                          factory.db (SQLite, WAL)
```

הפרסר של פורמט WhatsApp יושב **בשרת** (`parser.py`) — כדי שיהיה ניתן לבדיקה ביחידה ולשימוש עתידי (למשל ייבוא קובץ יצוא צ'אט שלם).

## 5. פריסה ותפעול

- **הרצה:** `python3 server.py` (ברירת מחדל: פורט 8342, מאזין על כל הכתובות ברשת). דורש Python ≥ 3.9, שום דבר אחר.
- **נתונים:** `factory.db` נוצר אוטומטית ליד השרת. **גיבוי = העתקת הקובץ** (מומלץ משימה יומית מתוזמנת).
- **בדיקות:** `python3 -m unittest` — מכסה את הפרסר ואת ה-API.
- **נתוני דוגמה:** `python3 seed.py` טוען את ארבע הבקשות מהצ'אט המקורי.

---

## 6. Blindspot Pass — ממצאים מתוך הדוגמאות עצמן

עברתי על ארבע הבקשות האמיתיות ששלחת ובדקתי איפה הפורמט "היפה" נשבר בפועל. כל ממצא כאן שינה את התכנון:

| # | ממצא בדוגמאות | ההשלכה על התכנון |
|---|---|---|
| **B1** | "כמות: חלק בודד" (צרצר) — כמות היא לא תמיד מספר | `quantity` הוא טקסט; אין ולידציה מספרית קשיחה |
| **B2** | "MBaz **(רשום תחת שועל מעופף)**" — לפרויקט יש שם רישום שונה במערכת | שדה `project_alias` נפרד, מוצג ומחופש |
| **B3** | אותו אדם בשמות שונים: "דימה"/"דימה גופמן", "רונן פירמן"/"רונן פירמן יפתח", "מתן"/"מתן חושקובר" | אין ישות "אנשים" קשיחה בשלב א'; חיפוש טקסטואלי. סומן כהרחבה (§8) — נרמול שמות |
| **B4** | מיקומי ייצור בטקסט חופשי ולא עקבי: "הית מלאכה תלת־ממד" (שגיא כתיב), "מסגריה / דגמים", "במדפסת fortus" | שדה מיקום = בחירה מרשימה מוצעת **או** טקסט חופשי; הרשימה נלמדת מהערכים הקיימים |
| **B5** | ההודעה של מתן נפתחת ב"**מאשר את הייצור?**" — בקשה נשלחה עם שם בקר לפני שהבקר אישר בפועל | אישור הוא **מצב במחזור חיים**, לא רק שדה טקסט. סטטוס "ממתינה לאישור" קיים בדיוק בגלל זה |
| **B6** | תאריך אישור 6.7 אבל ההודעה נשלחה 8.7; תג"ב 8.7 = יום שליחת ההודעה | תאריך אישור ≠ תאריך קליטה — שניהם נשמרים; תג"ב שכבר עבר ביום הקליטה מסומן מיד באדום |
| **B7** | פורמטי תאריך מעורבים: `22.6`, `21.6`, `6.7.26`, `15.07.26` | פרסר תאריכים סולח + נרמול ל-ISO + אישור חזותי של המשתמש בעת ייבוא |
| **B8** | "#1114" מופיע בתוך "שם ייצור במערכת" — יש מערכת נוספת עם מזהים משלה | שדה `external_id` נפרד; הפרסר שולף `#NNNN` אוטומטית |
| **B9** | הבקשה של צרצר **חתוכה באמצע** — אין "שם ייצור במערכת" ואין הערות | הפרסר חייב לשרוד קלט חלקי; בקשה חלקית נקלטת כ"מעוכבת — חוסרים" במקום להידחות |
| **B10** | תג"ב צמוד מאוד לאישור (אישור 6.7 → תג"ב 8.7): יומיים בלבד | לוח הבקרה מדגיש "תג"ב קרוב" ולא רק "עבר תג"ב" |

---

## 7. Unknown Unknowns — מה שאתה כנראה לא יודע שאתה לא יודע

אלה השאלות שלא שאלת, ולדעתי הן יקבעו אם המערכת תחיה או תמות:

1. **"שם ייצור במערכת" מסגיר שכבר יש מערכת.** יש לכם ERP/מערכת ייצור קיימת. האפליקציה הזו יוצרת **הזנה כפולה** — כל בקשה תוקלד גם שם וגם כאן. הזנה כפולה היא הסיבה מס' 1 שמערכות כאלה ננטשות תוך חודשיים. צריך להחליט: זו מערכת הקדם-אישור שמזינה את ה-ERP, או מראה שלו? (`external_id` הוא הוו שמאפשר חיבור עתידי.)

2. **הפורמט של יוסי נגמר ברגע שהבקשה נשלחה — ואין בו בכלל "סיום".** אין שדה "הושלם", אין מי שמדווח שהעבודה יצאה מהמסגריה. מי סוגר בקשות? אם אף אחד לא מוגדר כאחראי לסגירה, הלוח יתמלא ב"בייצור" נצחיים והמידע יהפוך לשקר. זו בעיה תהליכית, לא טכנית — והמערכת רק תחשוף אותה.

3. **WhatsApp לא ייעלם — ותהיה תקופה של שני מקורות אמת.** אם מישהו שולח בוואטסאפ ולא מזין, המערכת משקרת. ההגנה שבניתי: ייבוא-בהדבקה תוך 10 שניות. אבל ההחלטה הארגונית ("בקשה שלא במערכת לא נכנסת לתור") — רק יוסי יכול לקבל אותה.

4. **אישור בלי תמונת עומס הוא חותמת גומי.** הבקר מאשר תג"ב בלי לראות כמה עבודות כבר יושבות על המסגריה באותו שבוע. בשלב א' הלוח מציג עומס לפי מיקום — אבל תכנון קיבולת אמיתי (שעות מכונה) הוא עולם שלם שלא הוגדר.

5. **מה קורה כשבקשה משתנה אחרי אישור?** שינוי כמות/תג"ב אחרי שהבקר אישר — האם האישור עדיין תקף? כרגע: כל שינוי נרשם בהיסטוריה, ושינוי בשדה מהותי אחרי אישור מסומן. מדיניות אמיתית (אישור מחדש?) — החלטה שלכם.

6. **סיווג ואבטחה.** שמות פרויקטים ("שועל מעופף", MD, SBRU...) על שרת — באיזו רשת מותר לזה לשבת? מי מנהל את המכונה? אין כרגע הזדהות (§2). אם זה צריך לרוץ ברשת עם דרישות סיווג — זו שאלה לקב"ט, לא לקוד.

7. **גיבוי הוא קובץ אחד — וגם נקודת הכשל היחידה.** `factory.db` על מחשב אחד. מי מגבה, לאן, ומי בודק פעם בחודש שהגיבוי נפתח?

8. **ריבוי זהויות של אנשים (ממצא B3) ירעיל דוחות.** "כמה עבודות אישר דימה" ייתן תשובה שגויה כשהוא מופיע גם כ"דימה" וגם כ"דימה גופמן". בשלב א' זה נסבל; ברגע שרוצים דוחות — צריך טבלת אנשים.

9. **אין הגדרת "דחוף".** בדוגמאות רואים תג"ב של יומיים, אבל אין שדה עדיפות בפורמט. הוספתי שדה עדיפות — אבל מי מוסמך לסמן "מיידי" ומה זה אומר לתור? תהליך שלא קיים עדיין.

10. **מדידת הצלחה.** איך תדעו שהמערכת עובדת? הצעה: אחרי חודש — % בקשות שנקלטו מלאות מהפעם הראשונה, ו-% עבודות שעמדו בתג"ב. אם אין מדד, אין בקרה — רק טבלה יפה.

## 8. נקודות הרחבה מסומנות (לא בשלב א')

- הזדהות משתמשים (בסיסית או מול AD ארגוני) והרשאות (בקר בלבד מאשר)
- טבלת אנשים + נרמול שמות; טבלת פרויקטים מנוהלת
- קבצים מצורפים (שרטוטים, שבלונות — ר' הבקשה של מתן)
- התראות (מייל/אחר) על התקרבות תג"ב
- ייבוא קובץ יצוא צ'אט WhatsApp שלם (הפרסר כבר בנוי לזה)
- תכנון קיבולת לפי מיקום ייצור
- חיבור ל-ERP דרך `external_id`
